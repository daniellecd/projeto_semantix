Pasta disponibilizada pelo professor na plataforma do curso.

Apresenta a seguinte estrutura de arquivo:

```~> elastic/
/elastic
|-- docker-compose.yml
|-- pipeline
    -- logstash.conf
|-- settings
    -- elasticsearch.yml
    -- kibana.yml
    -- logstash.yml
